{
    "name": "@pulse.fm/server",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "test": "vitest run",
        "build": "tsc",
        "dev": "tsx --watch src/index.ts",
        "codegen": "kysely-codegen --camel-case"
    },
    "dependencies": {
        "kysely": "^0.28.9",
        "pg": "^8.16.3"
    },
    "devDependencies": {
        "@types/pg": "^8.16.0",
        "kysely-codegen": "^0.19.0"
    }
}
